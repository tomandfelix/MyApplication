<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/activity_margin"
    android:animateLayoutChanges="true">

        <TextView
            android:id="@+id/internet_received_data"
            android:textSize="@dimen/textSize"
            android:layout_width="match_parent"
            android:hint="waiting..."
            android:layout_height="44dp"
            android:layout_marginTop="@dimen/text_separation"/>
        <EditText
            android:id="@+id/internet_send_id"
            android:textSize="@dimen/textSize"
            android:hint="receiver id"
            android:inputType="number"
            android:singleLine="true"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginTop="@dimen/text_separation"/>

        <EditText
            android:id="@+id/internet_send_data"
            android:textSize="@dimen/textSize"
            android:hint="type here"
            android:singleLine="true"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginTop="@dimen/text_separation"/>
        <Button
            android:id="@+id/internet_send_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Send"
            android:background="@drawable/custom_button"
            android:textColor="@color/material_drawer_primary_text"
            android:onClick="toSend"/>

</LinearLayout>

