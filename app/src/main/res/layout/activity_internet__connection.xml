<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/activity_margin"
        android:animateLayoutChanges="true">

            <TextView
                android:id="@+id/receive_UDP_data"
                android:text="Waiting for UDP data"
                android:textSize="20sp"
                android:layout_width="match_parent"
                android:layout_height="44dp"/> />
            <TextView
                android:id="@+id/received_data"
                android:textSize="@dimen/textSize"
                android:layout_width="match_parent"
                android:hint="waiting..."
                android:layout_height="44dp"
                android:layout_marginTop="@dimen/text_separation"/>
            <TextView
                android:id="@+id/Send_UDP_data"
                android:text="Type and then press button to send"
                android:textSize="20sp"
                android:layout_width="match_parent"
                android:layout_height="44dp"/>
            <EditText
                android:id="@+id/send_data"
                android:textSize="@dimen/textSize"
                android:hint="type here"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:layout_marginTop="@dimen/text_separation"/>
            <Button
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Send"
                android:onClick="toSend"/>

    </LinearLayout>
</LinearLayout>

